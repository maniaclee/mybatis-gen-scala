-@ val package:String
-@ val table:psyco.db.TableInfo
package #{package};
public class #{table.className} implements Serializable {

-for(col <- #{table.columns}){
    private #{col.javaType} #{col.fieldName};
-}

}